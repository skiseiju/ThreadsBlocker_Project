# Session handoff — 2026-07-27

## 現在的狀態

- 分支 `main`，HEAD `be9d916`，工作樹乾淨。
- 最新測試版 **2.8.1-beta4**，已 build，**等使用者實機驗證封鎖成功率**。
- 全套測試：239 tests、236 pass、0 skip、3 fail。
- 那 3 個 fail 是 `beta54`／`beta58`／`beta59` 硬編 `2.7.4` 版號的斷言，`3cea5d0` 之前就存在，與本輪無關。stable 版號下它們會條件式 skip，beta 版號下會執行並失敗。

## 專案方向（使用者 2026-07-27 決定）

**2.8 起不開發新功能，只做修 bug 與拆結構。**

工作方式：**一個 bug 一個 commit**，每個 commit 由使用者實機 verify 通過後才做下一個。順序由 Orchestrator 安排，排序原則是「使用者驗得出來的優先、壓在封鎖成功率上的優先」。

## 本輪已完成（依時間序）

| commit | 內容 | 使用者驗證 |
|---|---|---|
| `0bfab09` | 封鎖進行中勾選框閃爍：六份狀態推導收斂為單一來源 | **已 pass**（2.8.1-beta1） |
| `3cea5d0` | 2.8.1-beta1 build | — |
| `eeb380c` | 診斷閘門收斂（SSOT #9） | 不可見，不需驗 |
| `7217d2f` | 清除選取改走 checkbox SSOT（SSOT #10） | 不可見，不需驗 |
| `04dc96e` | 匯入名單大小寫／hash 造成重複封鎖 | **已 pass**（2.8.1-beta2） |
| `a8dfaae` | 2.8.1-beta2 build | — |
| `e4aa24f` | 個人頁 action anchor 的 More 偵測改用共用 locator（SSOT #8） | **待驗** |
| `b8e7c28` | 2.8.1-beta3 build（**不完整，勿使用**） | — |
| `1e87523` | 移除 action anchor 的 legacy 幾何窗最終閘門（補 e4aa24f） | **待驗** |
| `be9d916` | 2.8.1-beta4 build | **待驗** |

### 待使用者驗證的項目（beta4）

封鎖成功率。重點情境：視窗縮小、瀏覽器縮放非 100%、側邊欄展開，以及使用者手上「一直封鎖失敗」的那批帳號。

驗證通過 → 接著做順序中的第 3 項。
驗證失敗 → 先看是否誤抓到下方貼文的 More（見下方殘留風險）。

## 本輪的教訓（下一個 session 必須遵守）

**在 luna 還在寫檔時 `git add` 會做出內部矛盾的 commit。** `e4aa24f` 就是這樣產生的：測試已含新期待，`src/core.js` 少了對應那一行，該 commit 自身的 beta50 套件是 7 pass / 1 fail。`1e87523` 才補齊。

**commit 前必須先確認 worker 已收工**，不能只看 `git status` 當下的樣子。作法：等 `.done.md` 出現且 sol 已回報彙整，再重跑一次測試，再 `git add`。

## 殘留風險（已記在 `1e87523` commit message）

個人頁 header 完全沒有 More 時，`findProfileActionAnchor` 理論上可能選到頁面下方貼文的 More。`MoreLocator` 在 `mode:'profile'` 對 `rect.top < 520` 加分、同分再依 top 由上而下排序，header 候選實務上仍會勝出。**此情境目前無測試覆蓋。**

使用者若回報「點下去開錯選單」，優先查這裡。

## 待辦順序（已與使用者確認）

來源：`docs/handoff/ssot-inventory.done.md`（12 項，基準 `3cea5d0`，行號在該 commit 上可驗證；`core.js` 後續有變動，動手前重新確認行號）。

### 已完成
- SSOT #8、#9、#10

### 下一個（第 3 項）
**抓彈出視窗統一（SSOT #4）。** 使用者可驗：開按讚名單、粉絲名單，看有沒有「視窗開了但抓不到人」。

已查明、可直接採用的結論：
- `src/features/report-flow.js` 與 `src/features/three-no-watch.js` **都沒有 import `DialogCollector`**，不是刻意寫不同契約，是各寫各的。
- `three-no-watch` 的 `findActiveFollowersDialog` 是共用版的簡化版，沒有額外好處，**直接改用共用版**。
- `report-flow` 的 `getDialogContext` 有一項比共用版更好的判定：會確認「這個 dialog 裡真的有我要找的那個 user」。**保留該驗證**，只把候選鏈裡的 `Core.getTopContext()`（取最後一個 dialog）這條錯誤退路處理掉。

### 接著
4. **帳號名稱全面統一小寫（SSOT #5）** — 使用者已決定一律轉小寫。證據：Threads 沿用 Instagram handle 規則，只允許小寫字母、數字、句點、底線，平台會把大寫靜默折為小寫，所以轉小寫是無損正規化。本輪只修了匯入路徑（`Core.parseImportedUsernames`），其餘解析器仍各自為政。
5. **判斷「有沒有在跑」（SSOT #1）** — canonical 是 `Core.resolveControllerStatus`，另有 7 處硬寫 10 秒、3 處硬寫 30 秒。收斂會改變行為（`stopping` 狀態），必須先列行為變化清單，且該保留的差異要改成顯性參數而非複製表達式。

### 之後（使用者可見度低）
6. 貼文網址正規化（SSOT #6）。**使用者已決定**：同一則貼文算一則，來源記成欄位；**舊資料不對齊**，新資料用新規則，觀測平台統計的短暫分裂可接受。
7. scroll root 三套規則（SSOT #7）。
8. 三份名單散讀 53 次（SSOT #2）。與勾選框閃爍同源，屬大工程，需先設計統一入口。

### 仍需使用者決定
9. **平台上傳 payload 有兩份完整推導（SSOT #3）。** 兩份內容已不一致，overlay 版少帶 three-no follower evidence。要砍掉一份，但需先確認哪一份正確、平台端有無依賴差異。**Orchestrator 應先查再給使用者選。**

### 未爆彈（最後）
10. skip-user 政策重複（SSOT #11）。
11. block/report ring 各自硬寫 48 小時保留窗（SSOT #12）。

## trio 狀態

- `surface:5` Claude（Orchestrator）
- `surface:6` sol，`gpt-5.6-sol` / medium（協調）
- `surface:7` luna，`gpt-5.6-luna` / max（實作）

兩者已請其待命。重新派工方式見 `~/.claude/skills/codex-trio/SKILL.md` 與 `/CODE/docs/TRIO_WORKFLOW.md`。

我曾就幾何窗閘門移除一事向 sol 發出查詢，**該問題已由 Orchestrator 自行查證並解決**（`1e87523`），sol 的回覆若晚到可忽略。

## 回話風格

使用者 2026-07-27 新增全域強制規則（`~/.claude/CLAUDE.md` 第零條「白話優先」）：第一次回答一律白話文，不出現檔名、行號、函式名、指令、程式碼區塊；追問才給細節；處理過程中不吐程式碼，只在最後給結論。
