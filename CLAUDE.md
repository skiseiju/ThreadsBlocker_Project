# ThreadsBlocker

本檔不另建第二套專案規則。接手本工作樹時，以同目錄 `AGENTS.md` 為操作、安全、版本與發布指令唯一來源，再讀 `PROJECT.md` 確認主 repo 與其他 worktree 的邊界，產品背景才查 `README.md` 與受影響的 `docs/`。

本工作樹是 ThreadsBlocker 主 repo 與正式發版來源；`ThreadsBlocker-analytics-upload` 是同專案的 incubator worktree。版本、build、artifact parity、installed truth、功能 smoke、storage／preference regression、正式發布批准與 rollback 都依 `AGENTS.md`，不得從 incubator 自動整批覆蓋主 repo。
