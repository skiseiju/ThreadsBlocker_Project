# Buglist

| # | 版本 | 狀態 | 摘要 | 根因 | 修復 |
|---|------|------|------|------|------|
| 1 | 2.5.0 | Fixed (2.5.1-beta2) | 定點絕第一次使用不會執行 — 「開始執行定點絕」按鈕被隱藏 | `hege_endless_worker_standby`（localStorage）跨 session 殘留，`isEndlessRunning` 誤判為 true。原因：功能上線時即缺少 startup 清理與 URL 不符時的 localStorage 同步清理 | main.js: (1) startup 時若 sessionStorage 無 endless_state 則清除殘留旗標 (2) URL 不符跳出時同步清除 localStorage 旗標 |
| 2 | 2.5.4-beta66 | Fixed (2.5.4-beta66) | 定點絕封鎖完會停在空名單頁，不會自動結束 worker | `worker.js` 在 `BG_QUEUE=0` 且 `hege_sweep_worker_standby=true` 時進入等待主視窗接續迴圈，特定分支不會收尾 | `worker.js`：空 queue 收尾分支改為直接結束（same-tab 回主頁、popup 直接關閉），不再停在空名單輪詢 |
| 3 | 2.5.4-beta67 | Fixed (2.5.4-beta67) | Menu 顯示「清單A 第N批 定點絕執行中」會殘留到 reload 才消失 | `updateControllerUI()` 只有寫入「執行中」字串，沒有在流程結束時清除 | `core.js`：加入收尾清理邏輯，當定點絕非執行中時即時重置狀態列 |
| 4 | 2.5.4-beta68 | Fixed (2.5.4-beta68) | 定點絕執行狀態在不同 UI/流程判斷不一致，偶發誤判或回錯頁 | 狀態機分散在 `ui.js` 與 `SweepDriver`；popup worker 收尾仍受 `hege_return_url` 影響；狀態清理依賴字串比對 | `utils.js` 新增 sweep runtime 單一判斷；`ui.js` 改為只讀共用判斷；`worker.js` popup 以 `hege_popup=true` 一律關閉；`core.js` 改用 `dataset` flag 清理狀態列 |
| 5 | 2.5.4-beta69 | Fixed (2.5.4-beta69) | 手動 reload 會誤續跑定點絕 | `main()` 啟動時只要命中 `WAIT_FOR_BG + target` 就直接 `waitForWorkerDrain()`，未檢查 runtime 是否真的可續跑 | `main.js`：加入 resume gate（target 狀態、runtime 條件、stale sweeping 自救）與詳細 debug log |
| 6 | 2.5.4-beta72 | Fixed (2.5.4-beta72) | 每次升版（版號更新）會固定觸發一次定點絕 | 升版 migration 把 `POST_QUEUE` 全量重設為 `pending`，深層清理項目被誤當新任務；首輪 tick/殘留 `hege_sweep` 參數會放大觸發機率 | `main.js`：改為「修復式 migration」不全量 reset；`longTermLoop + lastSweptAt + pending` 回正為 `done`；升版後跳過一次 auto tick；`hege_sweep` 參數需有 active runtime 才允許續跑 |
| 7 | 2.6.1-beta2 | Fixed (2.6.1-beta2) | 大蟑螂回望提醒名單過長時，確認/取消按鈕被推出視窗外，使用者一進頁面就卡住 | 回望提醒把所有逾期帳號直接塞進通用 confirm；通用 confirm 沒有限制內容高度，也沒有把 footer 固定在可視範圍 | `ui.js`：通用 confirm 改為視窗內 flex layout，內容可捲、footer 固定；`main.js`：回望提醒改為摘要名單、最多先開 10 個，確認或稍後提醒都會延後本批提醒避免重載立即再彈 |
| 8 | 2.6.4 | Fixed (2.6.4) | 大蟑螂回望提醒在小視窗/手機上仍可能因名單摘要太長而按不到確認或取消 | v2.6.1 雖限制最多列 30 個帳號，但 30 行在較矮 viewport 仍會壓縮 footer；通用 confirm 也缺少寬度、換行、安全區與按鈕換行保護 | `ui.js`：confirm modal 改為 viewport 內寬高、內容區強制捲動/換行、footer sticky 且支援 safe-area；`main.js`：回望摘要縮為 8 個帳號並保留「前 10 個」開啟上限 |
| 9 | 2.6.4 | Fixed (2.6.4) | 2.6.3 的平台上傳 raw 資料修復沒有合回主 repo，導致 duplicate 上傳仍可能只更新既有分析紀錄、不留下 raw 重傳資料 | 2.6.3 修正在 `ThreadsBlocker-analytics-upload` 工作樹完成並上傳 draft，但 main 工作樹仍停在 2.6.2 程式碼；server 端也以整份 raw JSON hash 當唯一鍵，duplicate 路徑直接 return | 合回 2.6.3 核心：新增 `platform_raw_ingests`；server 每次 ingest 先保存 raw payload，再以 canonical analysis hash 去重；client 升版後對已開啟自動上傳者跑一次 `repair_reupload_v1`，並延續舊版同意狀態 |
| 10 | 2.8.1-beta1 | Open（待修，排在 SSOT #4 之前或併入） | 只檢舉流程選完「這是垃圾訊息」後點到左側主導覽，不是檢舉選項 | `report-flow.js` 的 `findNextReportOption` 在 `document.querySelectorAll('div[role="dialog"]')` 為空時，把整份 `document` 當成搜尋 root（`const roots = dialogs.length > 0 ? [...dialogs, document] : [document]`）。dialog 已關閉或尚未開啟時，下一層選項的文字比對會掃到整頁，主導覽的「為你推薦／新串文／搜尋／訊息／動態／個人檔案」等項目都是可點文字節點，因此可能被當成檢舉選項點下去。使用者實測 log 顯示 `dialogs=0`、可見選項全部是左側導覽列。這是 fail-open，應為 fail-closed。同屬 SSOT inventory #4「dialog context 取法不一致」家族 | 待修。方向：`dialogs.length === 0` 時不得 fallback 到 `document`，應回 `null` 並讓上層重試或明確失敗；`getVisibleReportOptionTexts` 同樣不得以 `document` 當 root，否則診斷訊息會誤導。另需補 report flow 的 RuntimeDiagnostics 儀器：本次 log 中 report feature 完全沒有任何 entry，只有 `panel`／`selection`／`message_route`，出事時無法定位 |
| 11 | 2.8.1-beta4 | Open（待修，最高優先） | worker 視窗縮小後封鎖連續失敗；視窗維持正常大小時失敗率為 0，側邊欄展開不影響 | 使用者 2026-07-27 實測 beta4 回報。beta4 已移除 `findProfileActionAnchor` 的 legacy 幾何窗最終閘門（commit `1e87523`），但小視窗仍連續失敗，代表 More 按鈕定位仍有其他與 viewport 尺寸相關的假設未拆乾淨（`more-locator.js` 的 scoring 含幾何加權，且 `findProfileRoot` 以 `findProfileActionAnchor` 當 filter）。待取實機診斷確認是「找不到 More」還是「找到但點不到」 | 待修。已定位兩處仍與 viewport 尺寸相依的假設：`more-locator.js:142-143` profile 模式的評分仍用寫死像素（`rect.top < 520` 加分、`rect.left < 80` 扣 30 分），視窗變窄時個人頁欄位左移，真正的更多按鈕會被扣分而輸給錯的候選；`more-locator.js:58-65` 的 `isVisible` 要求元素落在 viewport 內，視窗變矮時折線下方的候選會被整個濾掉。先取證：小視窗跑一批並匯出 RuntimeDiagnostics 確認失敗 reason 分佈，再決定是拆掉幾何評分還是改用相對版面判斷 |
| 12 | 2.8.1-beta4 | Open（待修） | 按「重試失敗清單」後再次失敗，整份失敗清單被清空 | 兩個獨立機制都會造成清單歸零，需實機分辨：(A) `storage.js` 的 memory cache 是每個 window 各一份、無跨 context 失效機制。`core.js:4577` 的「重試全部」在主視窗把 `FAILED_QUEUE` 設為 `[]`（同時寫進主視窗 cache），worker 是另開視窗、寫回 localStorage 的新失敗紀錄主視窗看不到；主視窗之後任何一次 `setJSON(FAILED_QUEUE, ...)` 都會用 `[]` 覆蓋掉 worker 寫入的真實資料。(B) `worker.js:1755-1768` 的 `triggerCooldown` 會把 `FAILED_QUEUE` 併進 `COOLDOWN_QUEUE` 後清空，UI 上看起來就是失敗清單消失 | 待修。方向：(A) `FAILED_QUEUE`／`REPORT_FAILED_QUEUE` 這類跨 window 共用的 key 不得走 memory cache，或改用 `storage` event 做跨 context 失效；(B) 冷卻備份後 UI 要明示「已移入冷卻備份」而非顯示 0 |
| 13 | 2.8.1-beta4 | Open（待修，與 #10／#11 同源） | 檢舉時出現「按到了興趣（標籤）頁，跳過」 | 使用者看到的是 `report-flow.js:846` 的 `navigation_mismatch` 跳過訊息：點下所謂的「更多」之後，網址變成 `/tags` 或 `serp_type=tags`，`MoreLocator.routeMatches`（`more-locator.js:172`）判定為不安全路由而放棄該帳號。代表點到的根本不是更多按鈕，而是頁面上的標籤連結。與 #11 同一根因家族：更多按鈕的候選挑選在特定版面下會選錯 | 待修，與 #11 一起處理 |
