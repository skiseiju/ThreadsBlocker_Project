{
  "name": "threadsblocker-license-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "npm run test && npx wrangler deploy --dry-run --outdir /tmp/threadsblocker-license-worker-dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
